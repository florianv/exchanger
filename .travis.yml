language: php

php:
  - 7.1
  - 7.2
  - 7.3

env:
  matrix:
    - PREFER="--prefer-lowest"
    - PREFER="--prefer-dist"

sudo: false

cache:
  directories:
    - $HOME/.composer/cache

before_script:
  - composer update --no-interaction $PREFER

script:
  - composer test
